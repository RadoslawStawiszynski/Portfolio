# F9.7 Framer Motion Animations — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dodać płynne animacje wejścia (fade-in, slide-up, staggered list) do wszystkich 6 bloków portfolio przy użyciu Framer Motion 11.

**Architecture:** Jeden współdzielony komponent kliencki `AnimatedSection` obsługuje proste animacje fade-in + slide-up na poziomie sekcji. Bloki z listami (Experience, Skills, Education) konwertujemy na komponenty klienckie i używają bezpośrednio `motion.*` z Framer variants dla staggered efektu. Server Components (Hero, About, Contact) pozostają server-side i importują `AnimatedSection` jako child.

**Tech Stack:** `framer-motion@^11`, Next.js 15 App Router, TypeScript 5, Tailwind CSS 4

---

## Struktura plików

| Plik | Akcja | Co robi |
|------|-------|---------|
| `platform/src/components/ui/AnimatedSection.tsx` | **CREATE** | `"use client"` — `motion.div` fade-in + slide-up on scroll, przyjmuje `delay` prop |
| `platform/src/components/blocks/HeroBlock.tsx` | **MODIFY** | Każdy element (avatar, h1, subtitle, CTA) w osobnym `<AnimatedSection delay={N}>` |
| `platform/src/components/blocks/AboutBlock.tsx` | **MODIFY** | Sekcja opakowana w `<AnimatedSection>` |
| `platform/src/components/blocks/ExperienceBlock.tsx` | **MODIFY** | `"use client"` + `motion.ol` / `motion.li` z variants stagger |
| `platform/src/components/blocks/SkillsBlock.tsx` | **MODIFY** | `"use client"` + `motion.div` grid z variants stagger per kategoria |
| `platform/src/components/blocks/EducationBlock.tsx` | **MODIFY** | `"use client"` + `motion.div` z variants stagger per item |
| `platform/src/components/blocks/ContactBlock.tsx` | **MODIFY** | Sekcja opakowana w `<AnimatedSection>` |

---

## Task 1: Instalacja framer-motion

**Files:**
- Modify: `platform/package.json` (via npm install)

- [ ] **Krok 1: Zainstaluj paczkę**

```bash
cd /home/rspro/Dokumenty/1.CODE/2.Portfolio/platform
npm install framer-motion
```

Oczekiwany output: `added 1 package` (lub podobny), brak błędów.

- [ ] **Krok 2: Zweryfikuj instalację**

```bash
node -e "const p=require('./node_modules/framer-motion/package.json'); console.log('framer-motion:', p.version)"
```

Oczekiwany output: `framer-motion: 11.x.x`

- [ ] **Krok 3: Typecheck — baseline przed zmianami**

```bash
cd /home/rspro/Dokumenty/1.CODE/2.Portfolio/platform
npm run typecheck 2>&1
```

Oczekiwany output: brak błędów (czyste wyjście).

- [ ] **Krok 4: Commit**

```bash
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio add platform/package.json platform/package-lock.json
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio commit -m "chore(deps): install framer-motion (F9.7)"
```

---

## Task 2: Komponent AnimatedSection

**Files:**
- Create: `platform/src/components/ui/AnimatedSection.tsx`

- [ ] **Krok 1: Stwórz plik**

```tsx
// platform/src/components/ui/AnimatedSection.tsx
"use client";
import { motion } from "framer-motion";

interface Props {
  children: React.ReactNode;
  className?: string;
  delay?: number;
}

export function AnimatedSection({ children, className, delay = 0 }: Props) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 24 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: "-80px" }}
      transition={{ duration: 0.5, delay, ease: "easeOut" }}
      className={className}
    >
      {children}
    </motion.div>
  );
}
```

- [ ] **Krok 2: Typecheck**

```bash
cd /home/rspro/Dokumenty/1.CODE/2.Portfolio/platform && npm run typecheck 2>&1
```

Oczekiwany output: brak błędów.

- [ ] **Krok 3: Commit**

```bash
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio add platform/src/components/ui/AnimatedSection.tsx
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio commit -m "feat(ui): add AnimatedSection — fade-in slide-up on scroll (F9.7)"
```

---

## Task 3: Animacja HeroBlock

**Files:**
- Modify: `platform/src/components/blocks/HeroBlock.tsx`

Hero ma 4 elementy wchodzące kolejno: avatar (delay 0) → h1 (delay 0.1) → subtitle (delay 0.2) → CTA (delay 0.3).

- [ ] **Krok 1: Zaktualizuj HeroBlock.tsx**

```tsx
// platform/src/components/blocks/HeroBlock.tsx
import Image from "next/image";
import type { HeroData } from "@/types/blocks";
import { AnimatedSection } from "@/components/ui/AnimatedSection";

interface Props {
  data: unknown;
  portfolioSlug: string;
}

export function HeroBlock({ data }: Props) {
  const d = data as HeroData;
  return (
    <section className="min-h-[60vh] flex flex-col items-center justify-center text-center px-4 py-16 lg:py-24 bg-[var(--color-bg)]">
      {d.avatarUrl && (
        <AnimatedSection delay={0}>
          <Image
            src={d.avatarUrl}
            alt={d.title}
            width={128}
            height={128}
            className="w-24 h-24 lg:w-32 lg:h-32 rounded-full mb-6 object-cover border-4 border-[var(--color-accent)]"
          />
        </AnimatedSection>
      )}
      <AnimatedSection delay={0.1}>
        <h1 className="text-4xl lg:text-6xl font-bold text-[var(--color-primary)] mb-4 max-w-3xl">
          {d.title}
        </h1>
      </AnimatedSection>
      {d.subtitle && (
        <AnimatedSection delay={0.2}>
          <p className="text-lg lg:text-xl text-[var(--color-muted)] mb-8 max-w-2xl">
            {d.subtitle}
          </p>
        </AnimatedSection>
      )}
      {d.cta && (
        <AnimatedSection delay={0.3}>
          <a
            href={d.cta.href}
            className="inline-block bg-[var(--color-accent)] text-[var(--color-bg)] px-8 py-3 rounded-lg font-semibold hover:opacity-90 transition-opacity"
          >
            {d.cta.label}
          </a>
        </AnimatedSection>
      )}
    </section>
  );
}
```

- [ ] **Krok 2: Typecheck**

```bash
cd /home/rspro/Dokumenty/1.CODE/2.Portfolio/platform && npm run typecheck 2>&1
```

Oczekiwany output: brak błędów.

- [ ] **Krok 3: Commit**

```bash
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio add platform/src/components/blocks/HeroBlock.tsx
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio commit -m "feat(blocks): animate HeroBlock — staggered entry avatar/title/subtitle/cta (F9.7)"
```

---

## Task 4: Animacja AboutBlock

**Files:**
- Modify: `platform/src/components/blocks/AboutBlock.tsx`

AboutBlock — jedna animacja na całą sekcję (zdjęcie + tekst wchodzą razem).

- [ ] **Krok 1: Zaktualizuj AboutBlock.tsx**

```tsx
// platform/src/components/blocks/AboutBlock.tsx
import Image from "next/image";
import type { AboutData } from "@/types/blocks";
import { AnimatedSection } from "@/components/ui/AnimatedSection";

interface Props {
  data: unknown;
  portfolioSlug: string;
}

export function AboutBlock({ data }: Props) {
  const d = data as AboutData;
  return (
    <section className="py-16 px-4 bg-[var(--color-bg-alt)]">
      <AnimatedSection className="max-w-4xl mx-auto flex flex-col md:flex-row gap-10 items-center">
        {d.photoUrl && (
          <Image
            src={d.photoUrl}
            alt="Zdjęcie"
            width={160}
            height={160}
            className="w-40 h-40 rounded-full object-cover shrink-0 border-4 border-[var(--color-accent)]"
          />
        )}
        <div>
          <h2 className="text-2xl lg:text-3xl font-bold text-[var(--color-primary)] mb-4">
            O mnie
          </h2>
          <p className="text-[var(--color-text)] leading-relaxed whitespace-pre-line">
            {d.bio}
          </p>
        </div>
      </AnimatedSection>
    </section>
  );
}
```

- [ ] **Krok 2: Typecheck**

```bash
cd /home/rspro/Dokumenty/1.CODE/2.Portfolio/platform && npm run typecheck 2>&1
```

Oczekiwany output: brak błędów.

- [ ] **Krok 3: Commit**

```bash
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio add platform/src/components/blocks/AboutBlock.tsx
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio commit -m "feat(blocks): animate AboutBlock — fade-in slide-up (F9.7)"
```

---

## Task 5: Animacja ExperienceBlock

**Files:**
- Modify: `platform/src/components/blocks/ExperienceBlock.tsx`

Konwertujemy na `"use client"`. Lista `<ol>` używa Framer variants: kontener (`staggerChildren: 0.12`), każdy `<li>` jest `motion.li` z wariantem item.

- [ ] **Krok 1: Zaktualizuj ExperienceBlock.tsx**

```tsx
// platform/src/components/blocks/ExperienceBlock.tsx
"use client";
import { motion } from "framer-motion";
import type { ExperienceData } from "@/types/blocks";

interface Props {
  data: unknown;
  portfolioSlug: string;
}

const listVariants = {
  hidden: {},
  show: { transition: { staggerChildren: 0.12, delayChildren: 0.1 } },
};

const itemVariants = {
  hidden: { opacity: 0, y: 20 },
  show: { opacity: 1, y: 0, transition: { duration: 0.45, ease: "easeOut" } },
};

function formatPeriod(startDate: string, endDate?: string): string {
  const fmt = (d: string) => {
    const [y, m] = d.split("-");
    const months = ["sty","lut","mar","kwi","maj","cze","lip","sie","wrz","paź","lis","gru"];
    return m ? `${months[parseInt(m) - 1]} ${y}` : y;
  };
  return `${fmt(startDate)} — ${endDate ? fmt(endDate) : "obecnie"}`;
}

export function ExperienceBlock({ data }: Props) {
  const d = data as ExperienceData;
  return (
    <section className="py-16 px-4 bg-[var(--color-bg)]">
      <div className="max-w-3xl mx-auto">
        <h2 className="text-2xl lg:text-3xl font-bold text-[var(--color-primary)] mb-8">
          Doświadczenie
        </h2>
        <motion.ol
          variants={listVariants}
          initial="hidden"
          whileInView="show"
          viewport={{ once: true, margin: "-60px" }}
          className="relative border-l border-[var(--color-bg-alt)] space-y-10"
        >
          {d.items.map((item, i) => (
            <motion.li key={i} variants={itemVariants} className="ml-6">
              <span className="absolute -left-2 flex h-4 w-4 items-center justify-center rounded-full bg-[var(--color-accent)]" />
              <p className="text-sm text-[var(--color-muted)] mb-1">
                {formatPeriod(item.startDate, item.endDate)}
              </p>
              <h3 className="text-lg font-semibold text-[var(--color-primary)]">
                {item.role}
              </h3>
              <p className="text-[var(--color-secondary)] font-medium mb-2">
                {item.company}
              </p>
              {item.description && (
                <p className="text-[var(--color-text)] leading-relaxed">
                  {item.description}
                </p>
              )}
            </motion.li>
          ))}
        </motion.ol>
      </div>
    </section>
  );
}
```

- [ ] **Krok 2: Typecheck**

```bash
cd /home/rspro/Dokumenty/1.CODE/2.Portfolio/platform && npm run typecheck 2>&1
```

Oczekiwany output: brak błędów.

- [ ] **Krok 3: Commit**

```bash
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio add platform/src/components/blocks/ExperienceBlock.tsx
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio commit -m "feat(blocks): animate ExperienceBlock — staggered list items (F9.7)"
```

---

## Task 6: Animacja SkillsBlock

**Files:**
- Modify: `platform/src/components/blocks/SkillsBlock.tsx`

Konwertujemy na `"use client"`. Kategorie wchodzą z opóźnieniem staggered, każdy `<span>` z tagiem umiejętności ma dodatkowy stagger w ramach kategorii.

- [ ] **Krok 1: Zaktualizuj SkillsBlock.tsx**

```tsx
// platform/src/components/blocks/SkillsBlock.tsx
"use client";
import { motion } from "framer-motion";
import type { SkillsData } from "@/types/blocks";

interface Props {
  data: unknown;
  portfolioSlug: string;
}

const gridVariants = {
  hidden: {},
  show: { transition: { staggerChildren: 0.1, delayChildren: 0.05 } },
};

const categoryVariants = {
  hidden: { opacity: 0, y: 16 },
  show: { opacity: 1, y: 0, transition: { duration: 0.4, ease: "easeOut" } },
};

const tagVariants = {
  hidden: { opacity: 0, scale: 0.85 },
  show: { opacity: 1, scale: 1, transition: { duration: 0.25, ease: "easeOut" } },
};

export function SkillsBlock({ data }: Props) {
  const d = data as SkillsData;
  return (
    <section className="py-16 px-4 bg-[var(--color-bg-alt)]">
      <div className="max-w-4xl mx-auto">
        <h2 className="text-2xl lg:text-3xl font-bold text-[var(--color-primary)] mb-8">
          Umiejętności
        </h2>
        <motion.div
          variants={gridVariants}
          initial="hidden"
          whileInView="show"
          viewport={{ once: true, margin: "-60px" }}
          className="grid grid-cols-1 md:grid-cols-2 gap-8"
        >
          {d.categories.map((cat, i) => (
            <motion.div key={i} variants={categoryVariants}>
              <h3 className="text-sm font-semibold uppercase tracking-widest text-[var(--color-muted)] mb-3">
                {cat.name}
              </h3>
              <motion.div
                variants={{ hidden: {}, show: { transition: { staggerChildren: 0.05 } } }}
                className="flex flex-wrap gap-2"
              >
                {cat.skills.map((skill, j) => (
                  <motion.span
                    key={j}
                    variants={tagVariants}
                    className="px-3 py-1 text-sm rounded-full bg-[var(--color-bg)] text-[var(--color-text)] border border-[var(--color-bg-alt)]"
                  >
                    {skill}
                  </motion.span>
                ))}
              </motion.div>
            </motion.div>
          ))}
        </motion.div>
      </div>
    </section>
  );
}
```

- [ ] **Krok 2: Typecheck**

```bash
cd /home/rspro/Dokumenty/1.CODE/2.Portfolio/platform && npm run typecheck 2>&1
```

Oczekiwany output: brak błędów.

- [ ] **Krok 3: Commit**

```bash
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio add platform/src/components/blocks/SkillsBlock.tsx
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio commit -m "feat(blocks): animate SkillsBlock — staggered categories + skill tags (F9.7)"
```

---

## Task 7: Animacja EducationBlock

**Files:**
- Modify: `platform/src/components/blocks/EducationBlock.tsx`

Konwertujemy na `"use client"`. Karty edukacji wchodzą staggered.

- [ ] **Krok 1: Zaktualizuj EducationBlock.tsx**

```tsx
// platform/src/components/blocks/EducationBlock.tsx
"use client";
import { motion } from "framer-motion";
import type { EducationData } from "@/types/blocks";

interface Props {
  data: unknown;
  portfolioSlug: string;
}

const listVariants = {
  hidden: {},
  show: { transition: { staggerChildren: 0.1, delayChildren: 0.1 } },
};

const cardVariants = {
  hidden: { opacity: 0, y: 20 },
  show: { opacity: 1, y: 0, transition: { duration: 0.4, ease: "easeOut" } },
};

export function EducationBlock({ data }: Props) {
  const d = data as EducationData;
  return (
    <section className="py-16 px-4 bg-[var(--color-bg)]">
      <div className="max-w-3xl mx-auto">
        <h2 className="text-2xl lg:text-3xl font-bold text-[var(--color-primary)] mb-8">
          Wykształcenie
        </h2>
        <motion.div
          variants={listVariants}
          initial="hidden"
          whileInView="show"
          viewport={{ once: true, margin: "-60px" }}
          className="space-y-6"
        >
          {d.items.map((item, i) => (
            <motion.div
              key={i}
              variants={cardVariants}
              className="p-6 rounded-xl bg-[var(--color-bg-alt)]"
            >
              <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-1 mb-2">
                <h3 className="text-lg font-semibold text-[var(--color-primary)]">
                  {item.school}
                </h3>
                <span className="text-sm text-[var(--color-muted)]">
                  {item.startYear}–{item.endYear ?? "obecnie"}
                </span>
              </div>
              <p className="text-[var(--color-text)]">
                {item.degree} · {item.field}
              </p>
            </motion.div>
          ))}
        </motion.div>
      </div>
    </section>
  );
}
```

- [ ] **Krok 2: Typecheck**

```bash
cd /home/rspro/Dokumenty/1.CODE/2.Portfolio/platform && npm run typecheck 2>&1
```

Oczekiwany output: brak błędów.

- [ ] **Krok 3: Commit**

```bash
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio add platform/src/components/blocks/EducationBlock.tsx
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio commit -m "feat(blocks): animate EducationBlock — staggered cards (F9.7)"
```

---

## Task 8: Animacja ContactBlock

**Files:**
- Modify: `platform/src/components/blocks/ContactBlock.tsx`

ContactBlock zawiera już `ContactForm` który jest `"use client"`. Sekcja opakowana w `<AnimatedSection>`.

- [ ] **Krok 1: Zaktualizuj ContactBlock.tsx**

```tsx
// platform/src/components/blocks/ContactBlock.tsx
import type { ContactData } from "@/types/blocks";
import { ContactForm } from "@/components/ui/ContactForm";
import { AnimatedSection } from "@/components/ui/AnimatedSection";

interface Props {
  data: unknown;
  portfolioSlug: string;
}

export function ContactBlock({ data, portfolioSlug }: Props) {
  const d = data as ContactData;
  return (
    <section className="py-16 px-4 bg-[var(--color-bg-alt)]">
      <AnimatedSection className="max-w-3xl mx-auto">
        <h2 className="text-2xl lg:text-3xl font-bold text-[var(--color-primary)] mb-8">
          Kontakt
        </h2>
        <div className="flex flex-col lg:flex-row gap-12">
          <div className="space-y-3 shrink-0">
            {d.email && (
              <p className="text-[var(--color-text)]">
                <span className="text-[var(--color-muted)] mr-2">Email:</span>
                <a href={`mailto:${d.email}`} className="text-[var(--color-accent)] hover:underline">
                  {d.email}
                </a>
              </p>
            )}
            {d.phone && (
              <p className="text-[var(--color-text)]">
                <span className="text-[var(--color-muted)] mr-2">Tel:</span>
                <a href={`tel:${d.phone}`} className="hover:underline">
                  {d.phone}
                </a>
              </p>
            )}
            {d.linkedin && (
              <p className="text-[var(--color-text)]">
                <span className="text-[var(--color-muted)] mr-2">LinkedIn:</span>
                <a href={d.linkedin} target="_blank" rel="noreferrer" className="text-[var(--color-accent)] hover:underline">
                  Profil
                </a>
              </p>
            )}
            {d.github && (
              <p className="text-[var(--color-text)]">
                <span className="text-[var(--color-muted)] mr-2">GitHub:</span>
                <a href={d.github} target="_blank" rel="noreferrer" className="text-[var(--color-accent)] hover:underline">
                  Profil
                </a>
              </p>
            )}
          </div>
          {d.showForm && <ContactForm portfolioSlug={portfolioSlug} />}
        </div>
      </AnimatedSection>
    </section>
  );
}
```

- [ ] **Krok 2: Typecheck**

```bash
cd /home/rspro/Dokumenty/1.CODE/2.Portfolio/platform && npm run typecheck 2>&1
```

Oczekiwany output: brak błędów.

- [ ] **Krok 3: Commit**

```bash
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio add platform/src/components/blocks/ContactBlock.tsx
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio commit -m "feat(blocks): animate ContactBlock — fade-in slide-up (F9.7)"
```

---

## Task 9: Weryfikacja finalna

**Files:** brak zmian kodu — tylko weryfikacja

- [ ] **Krok 1: Lint**

```bash
cd /home/rspro/Dokumenty/1.CODE/2.Portfolio/platform && npm run lint 2>&1
```

Oczekiwany output: `✔ No ESLint warnings or errors`

- [ ] **Krok 2: Typecheck finalny**

```bash
cd /home/rspro/Dokumenty/1.CODE/2.Portfolio/platform && npm run typecheck 2>&1
```

Oczekiwany output: brak błędów.

- [ ] **Krok 3: Weryfikacja w przeglądarce**

Otwórz `http://localhost:3000/dev/radek` (lub aktywny port).

Sprawdź kolejno:
- HeroBlock: avatar/title/subtitle/CTA wchodzą sekwencyjnie z opóźnieniem ~100ms
- AboutBlock: sekcja pojawia się po scrollu (fade-in + slide-up)
- ExperienceBlock: elementy osi czasu wchodzą jeden po drugim (stagger ~120ms)
- SkillsBlock: kategorie wchodzą, następnie tagi scale-in w kategorii
- EducationBlock: karty wchodzą staggered
- ContactBlock: sekcja pojawia się po scrollu

Jeśli animacje się nie wywołują przy pierwszym ładowaniu — `viewport: { once: true }` działa poprawnie (animują się tylko raz).

- [ ] **Krok 4: Oznacz task w PLAN.md**

W `PLAN.md` znajdź linię `F9.7` i zmień `[ ]` na `[x]`:

```
- [x] **F9.7** Płynne animacje (Framer Motion: fade-in, slide-up, staggered) (2026-06-14, Agent: Claude)
```

W sekcji Roadmap (§20) zaktualizuj status Fazy 2 dla animacji.

- [ ] **Krok 5: Zaktualizuj CHANGELOG.md**

Dodaj pod `## [Unreleased]` → `### Added`:

```markdown
- Animacje wejścia Framer Motion 11: fade-in + slide-up dla wszystkich bloków portfolio (F9.7)
- `AnimatedSection` — współdzielony klient wrapper (`whileInView`, `once: true`) dla Hero, About, Contact
- Staggered list animations w ExperienceBlock, SkillsBlock (tagi), EducationBlock (karty)
```

- [ ] **Krok 6: Push**

```bash
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio add PLAN.md CHANGELOG.md
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio commit -m "docs(sync): mark F9.7 done — Framer Motion animations"
git -C /home/rspro/Dokumenty/1.CODE/2.Portfolio push origin dev
```

---

## Self-Review

**Spec coverage:**
- ✅ fade-in — `AnimatedSection` (opacity: 0 → 1)
- ✅ slide-up — `AnimatedSection` (y: 24 → 0)
- ✅ staggered — ExperienceBlock (ol/li), SkillsBlock (grid/tags), EducationBlock (cards), HeroBlock (delay per element)
- ✅ Wszystkie 6 bloków animowane

**Placeholder scan:** Brak — każdy krok zawiera kompletny kod.

**Type consistency:**
- `AnimatedSection` przyjmuje `children: React.ReactNode`, `className?: string`, `delay?: number` — spójne z użyciem w Task 3, 4, 8
- `motion.*` z Framer 11 — `variants`, `initial`, `whileInView`, `viewport` — API spójne we wszystkich taskach
- `listVariants` / `itemVariants` / `cardVariants` — zdefiniowane lokalnie w każdym bloku, brak cross-task dependency
