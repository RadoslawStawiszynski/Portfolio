# B8.5 Contact Form API — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement `POST /api/contact` — endpoint formularza kontaktowego z walidacją Zod, rate limitingiem Redis (3 req/15 min per IP) i wysyłką emaili przez Resend.

**Architecture:** Route Handler w `app/api/contact/route.ts`. Redis singleton w `lib/redis.ts`, helper rate-limit w `lib/rate-limit.ts`. Pole `contactEmail` dodane do kolekcji Portfolios. Brak frameworka testowego — weryfikacja przez curl + TypeScript typecheck.

**Tech Stack:** Next.js 15 App Router, `@upstash/redis`, `resend`, `zod`, Payload CMS 3 (DB lookup), pino (logger)

**Spec:** `docs/superpowers/specs/2026-06-13-contact-api-design.md`

---

## File Map

| Akcja | Plik |
|-------|------|
| Modify | `platform/src/payload/collections/Portfolios.ts` |
| Create | `platform/src/lib/redis.ts` |
| Create | `platform/src/lib/rate-limit.ts` |
| Modify | `platform/src/lib/index.ts` |
| Create | `platform/src/app/api/contact/route.ts` |

---

## Task 1: Zainstaluj zależności

**Files:** `platform/package.json` (auto-updated by npm)

- [ ] **Step 1: Zainstaluj pakiety**

```bash
cd platform
npm install resend @upstash/redis zod
```

Expected output: `added N packages` bez błędów.

- [ ] **Step 2: Zweryfikuj w package.json**

```bash
grep -E '"resend"|"@upstash/redis"|"zod"' package.json
```

Expected:
```
"@upstash/redis": "^x.x.x",
"resend": "^x.x.x",
"zod": "^x.x.x",
```

- [ ] **Step 3: Commit**

```bash
git add platform/package.json platform/package-lock.json
git commit -m "chore(deps): install resend, @upstash/redis, zod (B8.5)"
```

---

## Task 2: Dodaj pole contactEmail do kolekcji Portfolios

**Files:**
- Modify: `platform/src/payload/collections/Portfolios.ts`

- [ ] **Step 1: Dodaj pole po `isPublished`**

W `platform/src/payload/collections/Portfolios.ts` dodaj na końcu tablicy `fields`, po bloku `isPublished`:

```typescript
    {
      name: "contactEmail",
      type: "email",
      defaultValue: "biuro@korp-cbm.com",
      admin: {
        description:
          "Email do odbierania wiadomości z formularza kontaktowego.",
      },
    },
```

- [ ] **Step 2: Zregeneruj typy Payload**

```bash
cd platform
npx payload generate:types
```

Expected: plik `platform/payload-types.ts` zaktualizowany — typ `Portfolio` ma nowe pole `contactEmail?: string | null`.

Jeśli polecenie nie istnieje, uruchom dev server (`npm run dev`) i zatrzymaj go — typy są generowane przy starcie.

- [ ] **Step 3: Sprawdź że typ istnieje**

```bash
grep "contactEmail" platform/payload-types.ts
```

Expected: `contactEmail?: string | null;`

- [ ] **Step 4: Commit**

```bash
git add platform/src/payload/collections/Portfolios.ts platform/payload-types.ts
git commit -m "feat(payload): add contactEmail field to Portfolios collection (B8.5)"
```

---

## Task 3: Utwórz Redis singleton

**Files:**
- Create: `platform/src/lib/redis.ts`
- Modify: `platform/src/lib/index.ts`

- [ ] **Step 1: Utwórz `platform/src/lib/redis.ts`**

```typescript
import { Redis } from "@upstash/redis";

if (!process.env.UPSTASH_REDIS_REST_URL || !process.env.UPSTASH_REDIS_REST_TOKEN) {
  throw new Error("Missing UPSTASH_REDIS_REST_URL or UPSTASH_REDIS_REST_TOKEN");
}

export const redis = new Redis({
  url: process.env.UPSTASH_REDIS_REST_URL,
  token: process.env.UPSTASH_REDIS_REST_TOKEN,
});
```

- [ ] **Step 2: Wyeksportuj z `platform/src/lib/index.ts`**

Dodaj na końcu pliku:

```typescript
export { redis } from "./redis";
```

- [ ] **Step 3: TypeScript check**

```bash
cd platform
npx tsc --noEmit
```

Expected: brak błędów.

- [ ] **Step 4: Commit**

```bash
git add platform/src/lib/redis.ts platform/src/lib/index.ts
git commit -m "feat(lib): add Upstash Redis singleton (B8.5)"
```

---

## Task 4: Utwórz helper rate-limit

**Files:**
- Create: `platform/src/lib/rate-limit.ts`
- Modify: `platform/src/lib/index.ts`

- [ ] **Step 1: Utwórz `platform/src/lib/rate-limit.ts`**

```typescript
import { redis } from "./redis";

const LIMIT = 3;
const WINDOW_SECONDS = 900; // 15 minut

export async function checkRateLimit(
  ip: string
): Promise<{ allowed: boolean; remaining: number }> {
  const key = `rate-limit:contact:${ip}`;
  const count = await redis.incr(key);
  if (count === 1) {
    await redis.expire(key, WINDOW_SECONDS);
  }
  return {
    allowed: count <= LIMIT,
    remaining: Math.max(0, LIMIT - count),
  };
}
```

- [ ] **Step 2: Wyeksportuj z `platform/src/lib/index.ts`**

Dodaj na końcu pliku:

```typescript
export { checkRateLimit } from "./rate-limit";
```

- [ ] **Step 3: TypeScript check**

```bash
cd platform
npx tsc --noEmit
```

Expected: brak błędów.

- [ ] **Step 4: Commit**

```bash
git add platform/src/lib/rate-limit.ts platform/src/lib/index.ts
git commit -m "feat(lib): add rate-limit helper — 3 req/15 min per IP (B8.5)"
```

---

## Task 5: Utwórz Route Handler POST /api/contact

**Files:**
- Create: `platform/src/app/api/contact/route.ts`

- [ ] **Step 1: Utwórz `platform/src/app/api/contact/route.ts`**

```typescript
import { NextRequest, NextResponse } from "next/server";
import { z } from "zod";
import { Resend } from "resend";
import { getPayload } from "payload";
import config from "@payload-config";
import { logger } from "@/lib/logger";
import { checkRateLimit } from "@/lib/rate-limit";

const ContactSchema = z.object({
  portfolioSlug: z.string().min(1),
  name: z.string().min(2).max(100),
  email: z.string().email(),
  message: z.string().min(10).max(2000),
});

const resend = new Resend(process.env.RESEND_API_KEY);

export async function POST(req: NextRequest) {
  // 1. Walidacja body
  let body: unknown;
  try {
    body = await req.json();
  } catch {
    return NextResponse.json({ error: "invalid_json" }, { status: 400 });
  }

  const parsed = ContactSchema.safeParse(body);
  if (!parsed.success) {
    return NextResponse.json(
      { error: "validation", fields: parsed.error.flatten().fieldErrors },
      { status: 400 }
    );
  }

  const { portfolioSlug, name, email, message } = parsed.data;

  // 2. Rate limiting
  const ip =
    req.headers.get("x-forwarded-for")?.split(",")[0]?.trim() ?? "unknown";

  const { allowed, remaining } = await checkRateLimit(ip);
  if (!allowed) {
    logger.warn({ ip, portfolioSlug }, "Rate limit exceeded on /api/contact");
    return NextResponse.json(
      { error: "rate_limit_exceeded" },
      {
        status: 429,
        headers: { "Retry-After": "900" },
      }
    );
  }

  // 3. Pobierz portfolio z DB
  const payload = await getPayload({ config });
  const result = await payload.find({
    collection: "portfolios",
    where: { subdomain: { equals: portfolioSlug } },
    limit: 1,
  });

  if (result.totalDocs === 0) {
    return NextResponse.json(
      { error: "portfolio_not_found" },
      { status: 404 }
    );
  }

  const portfolio = result.docs[0];
  const contactEmail =
    portfolio.contactEmail || "biuro@korp-cbm.com";

  // 4. Wyślij email
  try {
    await resend.emails.send({
      from: process.env.RESEND_FROM_EMAIL ?? "noreply@korp-cbm.com",
      to: contactEmail,
      subject: `Nowa wiadomość z portfolio ${portfolioSlug}`,
      text: `Imię: ${name}\nEmail nadawcy: ${email}\n\n${message}`,
    });
  } catch (err) {
    logger.error({ err, portfolioSlug }, "Failed to send contact email");
    return NextResponse.json({ error: "email_failed" }, { status: 500 });
  }

  logger.info(
    { portfolioSlug, ip, remaining },
    "Contact email sent successfully"
  );

  return NextResponse.json({ success: true }, { status: 200 });
}
```

- [ ] **Step 2: TypeScript check**

```bash
cd platform
npx tsc --noEmit
```

Expected: brak błędów.

- [ ] **Step 3: Commit**

```bash
git add platform/src/app/api/contact/route.ts
git commit -m "feat(api): POST /api/contact — Zod validation, rate limit, Resend (B8.5)"
```

---

## Task 6: Weryfikacja manualna (curl)

**Wymagania:** dev server uruchomiony (`npm run dev` w `platform/`), Docker z PostgreSQL i Redis działa lokalnie.

- [ ] **Step 1: Uruchom serwer**

```bash
cd platform
docker compose -f docker-compose.dev.yml up -d
npm run dev
```

Expected: serwer na http://localhost:3000

- [ ] **Step 2: Test 400 — błąd walidacji**

```bash
curl -s -X POST http://localhost:3000/api/contact \
  -H "Content-Type: application/json" \
  -d '{"portfolioSlug":"test","name":"A","email":"not-an-email","message":"hi"}' | python3 -m json.tool
```

Expected:
```json
{
  "error": "validation",
  "fields": {
    "name": ["String must contain at least 2 character(s)"],
    "email": ["Invalid email"],
    "message": ["String must contain at least 10 character(s)"]
  }
}
```

- [ ] **Step 3: Test 404 — nieznany slug**

```bash
curl -s -X POST http://localhost:3000/api/contact \
  -H "Content-Type: application/json" \
  -d '{"portfolioSlug":"nonexistent","name":"Jan Kowalski","email":"jan@example.com","message":"Dzień dobry, mam pytanie o współpracę."}' | python3 -m json.tool
```

Expected:
```json
{"error": "portfolio_not_found"}
```

- [ ] **Step 4: Utwórz testowe portfolio w Payload admin**

Otwórz http://localhost:3000/admin → Portfolios → Create new:
- subdomain: `test`
- owner: (dowolny user)
- contactEmail: `twoj@email.com`
- isPublished: zaznacz

- [ ] **Step 5: Test 200 — happy path**

```bash
curl -s -X POST http://localhost:3000/api/contact \
  -H "Content-Type: application/json" \
  -d '{"portfolioSlug":"test","name":"Jan Kowalski","email":"jan@example.com","message":"Dzień dobry, mam pytanie o współpracę."}' | python3 -m json.tool
```

Expected:
```json
{"success": true}
```

Sprawdź skrzynkę emailową `twoj@email.com`.

- [ ] **Step 6: Test 429 — rate limit**

Wyślij 4 razy ten sam request (3 przejdą, 4. powinien zwrócić 429):

```bash
for i in 1 2 3 4; do
  echo "Request $i:"
  curl -s -X POST http://localhost:3000/api/contact \
    -H "Content-Type: application/json" \
    -d '{"portfolioSlug":"test","name":"Jan Kowalski","email":"jan@example.com","message":"Dzień dobry, mam pytanie."}' | python3 -m json.tool
done
```

Expected: requesty 1–3 → `{"success": true}`, request 4 → `{"error": "rate_limit_exceeded"}` z HTTP 429.

- [ ] **Step 7: Oznacz task w PLAN.md jako ukończony**

W `PLAN.md` zmień:
```
- [ ] **B8.5** API endpoint formularz kontaktowy z rate limiting (Redis)
```
na:
```
- [x] **B8.5** API endpoint formularz kontaktowy z rate limiting (Redis) (2026-06-13, Agent: Claude)
```

- [ ] **Step 8: Aktualizuj CHANGELOG.md**

Dodaj wpis w sekcji `[Unreleased]` lub nowej sekcji daty:

```markdown
### Added
- `POST /api/contact` — endpoint formularza kontaktowego z walidacją Zod, rate limitingiem Redis (3 req/15 min per IP) i wysyłką emaili przez Resend (B8.5)
- Pole `contactEmail` w kolekcji Portfolios — edytowalne w admin panelu
- `lib/redis.ts` — singleton Upstash Redis
- `lib/rate-limit.ts` — helper rate limitingu per IP
```

- [ ] **Step 9: Final commit**

```bash
git add PLAN.md CHANGELOG.md
git commit -m "docs: mark B8.5 done — POST /api/contact with rate limiting"
```
