# F9.14 Landing Page Platformy Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Zastąpić placeholder na `korp-cbm.com` (i `localhost:3000`) prawdziwą landing page platformy PortfolioHub z sekcjami: Hero, Funkcje, Aktywne Portfolio i Footer.

**Architecture:** Tworzymy czysty Server Component `LandingPage` w `platform/src/components/landing/LandingPage.tsx`. Komponent korzysta z istniejących CSS Custom Properties (`:root` light theme, bo brak `data-theme` na `<html>` gdy `slug` jest null). `(portfolio)/page.tsx` importuje go i renderuje w gałęzi `!slug`. Brak nowych zależności.

**Tech Stack:** Next.js 15 Server Component, Tailwind CSS 4 z CSS Custom Properties (`var(--color-*)`), istniejące zmienne `:root` (light theme)

---

## Context

**Routing:** Middleware (`platform/src/middleware.ts`) ustawia `x-portfolio-slug` tylko dla subdomain lub `/dev/[slug]`. Gdy odwiedzamy `korp-cbm.com` (prod) lub `localhost:3000` bez ścieżki `/dev/` (dev) — slug jest `null`.

**Layout** (`platform/src/app/(portfolio)/layout.tsx`): Gdy `slug === null` — brak `data-theme` na `<html>`, brak ThemeToggle/DownloadCvButton/CookieConsentBanner. CSS Variables `:root` (light theme) obowiązują.

**Obecny placeholder** w `platform/src/app/(portfolio)/page.tsx`:
```tsx
if (!slug) {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-24">
      <h1 className="text-4xl font-bold text-(--color-primary)">PortfolioHub</h1>
      <p className="text-lg text-(--color-muted)">
        Multi-user portfolio platform
      </p>
    </main>
  );
}
```
Ten fragment zastępujemy `<LandingPage />`.

**CSS Custom Properties (`:root` — light theme):**
```
--color-bg:      #f5f3f2   (ciepły off-white)
--color-bg-alt:  #d8d2cf   (nieco ciemniejszy)
--color-primary: #1a1f00   (prawie czarny)
--color-accent:  #e19d29   (złoty amber)
--color-muted:   #8d8179   (stonowany brąz)
--color-text:    #1a1f00
```

**Domena:** `process.env.NEXT_PUBLIC_PLATFORM_DOMAIN ?? "korp-cbm.com"` (Server Component może czytać `process.env` bezpośrednio).

---

## File Map

| File | Action | Purpose |
|------|--------|---------|
| `platform/src/components/landing/LandingPage.tsx` | Create | Server Component — Hero + Features + Examples + Footer |
| `platform/src/app/(portfolio)/page.tsx` | Modify | Import `LandingPage`, zastąpić placeholder |

---

### Task 1: Create LandingPage component + wire into page.tsx

**Files:**
- Create: `platform/src/components/landing/LandingPage.tsx`
- Modify: `platform/src/app/(portfolio)/page.tsx`

- [ ] **Step 1: Create `platform/src/components/landing/LandingPage.tsx`**

```tsx
// platform/src/components/landing/LandingPage.tsx

const PLATFORM_DOMAIN =
  process.env.NEXT_PUBLIC_PLATFORM_DOMAIN ?? "korp-cbm.com";

const FEATURES = [
  {
    icon: "🧩",
    title: "Blokowa budowa",
    body: "Hero, O mnie, Doświadczenie, Umiejętności, Edukacja, Kontakt — każdy blok edytowalny w panelu admina bez dotykania kodu.",
  },
  {
    icon: "🎨",
    title: "10 motywów",
    body: "Od klasycznego jasnego przez Earth i Synthwave po Retro Terminal. Motywy przełączają się bez przeładowania strony.",
  },
  {
    icon: "🌐",
    title: "Własna domena",
    body: "Subdomeny (*.korp-cbm.com) lub własna domena przez rekord CNAME — zero konfiguracji po stronie platformy.",
  },
  {
    icon: "📬",
    title: "Formularz kontaktowy",
    body: "Walidacja Zod, ochrona rate-limit, wysyłka przez Resend. Gotowe do użycia po podaniu adresu email w adminie.",
  },
];

const EXAMPLES = [
  {
    name: "Radosław Stawiszyński",
    role: "Product Manager / Scrum Master",
    slug: "radek",
  },
  {
    name: "Miłosz Gawlik",
    role: "Portfolio IT",
    slug: "milosz",
  },
  {
    name: "Martyna Stawiszyńska",
    role: "Autorka książek",
    slug: "martyna",
  },
];

export function LandingPage() {
  return (
    <div className="min-h-screen bg-[var(--color-bg)] text-[var(--color-text)]">
      {/* ── Hero ── */}
      <section className="flex min-h-screen flex-col items-center justify-center px-6 py-24 text-center">
        <p className="mb-4 text-sm font-semibold uppercase tracking-widest text-[var(--color-accent)]">
          Portfolio Platform
        </p>
        <h1 className="text-5xl font-bold leading-tight text-[var(--color-primary)] md:text-7xl">
          Portfolio
          <span className="text-[var(--color-accent)]">Hub</span>
        </h1>
        <p className="mt-6 max-w-xl text-lg text-[var(--color-muted)] md:text-xl">
          Profesjonalne portfolio dla każdego. Własna domena, edytowalne bloki,
          wiele motywów — gotowe w minuty.
        </p>
        <div className="mt-10 flex flex-wrap justify-center gap-4">
          <a
            href={`https://radek.${PLATFORM_DOMAIN}`}
            className="rounded-full bg-[var(--color-accent)] px-6 py-3 text-sm font-semibold text-[var(--color-bg)] shadow hover:opacity-90 transition-opacity"
          >
            Zobacz przykład →
          </a>
          <a
            href="/admin"
            className="rounded-full border border-[var(--color-bg-alt)] px-6 py-3 text-sm font-semibold text-[var(--color-text)] hover:bg-[var(--color-bg-alt)] transition-colors"
          >
            Panel admina
          </a>
        </div>
      </section>

      {/* ── Features ── */}
      <section className="bg-[var(--color-bg-alt)] px-6 py-20">
        <div className="mx-auto max-w-5xl">
          <h2 className="mb-12 text-center text-3xl font-bold text-[var(--color-primary)]">
            Co oferuje platforma?
          </h2>
          <div className="grid gap-8 sm:grid-cols-2">
            {FEATURES.map((f) => (
              <div
                key={f.title}
                className="rounded-2xl bg-[var(--color-bg)] p-6 shadow-sm"
              >
                <div className="mb-3 text-3xl" aria-hidden="true">
                  {f.icon}
                </div>
                <h3 className="mb-2 text-lg font-semibold text-[var(--color-primary)]">
                  {f.title}
                </h3>
                <p className="text-sm leading-relaxed text-[var(--color-muted)]">
                  {f.body}
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* ── Examples ── */}
      <section className="px-6 py-20">
        <div className="mx-auto max-w-5xl">
          <h2 className="mb-12 text-center text-3xl font-bold text-[var(--color-primary)]">
            Aktywne portfolio
          </h2>
          <div className="grid gap-6 sm:grid-cols-3">
            {EXAMPLES.map((e) => (
              <a
                key={e.slug}
                href={`https://${e.slug}.${PLATFORM_DOMAIN}`}
                className="group rounded-2xl border border-[var(--color-bg-alt)] p-6 transition-colors hover:border-[var(--color-accent)]"
              >
                <div className="mb-3 h-10 w-10 rounded-full bg-[var(--color-accent)] opacity-70 transition-opacity group-hover:opacity-100" />
                <p className="font-semibold text-[var(--color-primary)]">
                  {e.name}
                </p>
                <p className="mt-1 text-sm text-[var(--color-muted)]">
                  {e.role}
                </p>
                <p className="mt-3 text-sm font-medium text-[var(--color-accent)]">
                  {e.slug}.{PLATFORM_DOMAIN} →
                </p>
              </a>
            ))}
          </div>
        </div>
      </section>

      {/* ── Footer ── */}
      <footer className="border-t border-[var(--color-bg-alt)] px-6 py-8 text-center text-sm text-[var(--color-muted)]">
        <p>
          PortfolioHub &copy; {new Date().getFullYear()} &mdash;{" "}
          <a
            href="/admin"
            className="transition-colors hover:text-[var(--color-accent)]"
          >
            Panel admina
          </a>
        </p>
      </footer>
    </div>
  );
}
```

- [ ] **Step 2: Read current page.tsx to find replacement point**

```bash
cat "platform/src/app/(portfolio)/page.tsx"
```

Find the `if (!slug)` block — it currently renders `<main>` with placeholder text.

- [ ] **Step 3: Modify `platform/src/app/(portfolio)/page.tsx`**

Replace the entire file with:

```tsx
// platform/src/app/(portfolio)/page.tsx
import type { Metadata } from "next";
import { headers } from "next/headers";
import { notFound } from "next/navigation";
import {
  getPortfolioBySlug,
  getBlocksBySlug,
  buildPortfolioMetadata,
} from "@/lib/portfolio";
import { PortfolioRenderer } from "@/components/blocks/PortfolioRenderer";
import { LandingPage } from "@/components/landing/LandingPage";

export async function generateMetadata(): Promise<Metadata> {
  const slug = (await headers()).get("x-portfolio-slug");
  if (!slug) return {};

  const portfolio = await getPortfolioBySlug(slug);
  if (!portfolio) return {};

  return buildPortfolioMetadata(portfolio, slug);
}

export default async function PortfolioPage() {
  const slug = (await headers()).get("x-portfolio-slug");

  if (!slug) {
    return <LandingPage />;
  }

  const [portfolio, blocks] = await Promise.all([
    getPortfolioBySlug(slug),
    getBlocksBySlug(slug),
  ]);

  if (!portfolio) notFound();

  return <PortfolioRenderer blocks={blocks} portfolioSlug={slug} />;
}
```

- [ ] **Step 4: Run typecheck**

```bash
cd platform && npm run typecheck 2>&1 | tail -20
```

Expected: zero errors. Common pitfalls:
- `process.env.NEXT_PUBLIC_PLATFORM_DOMAIN` — typ `string | undefined`, obsłużone przez `?? "korp-cbm.com"` ✅
- `FEATURES` i `EXAMPLES` jako `const` tablice literałów — TS infers typów automatycznie ✅
- `new Date().getFullYear()` — poprawne w Server Component, renderowane na serwerze ✅

- [ ] **Step 5: Commit**

```bash
git add platform/src/components/landing/LandingPage.tsx "platform/src/app/(portfolio)/page.tsx"
git commit -m "feat(landing): LandingPage — Hero, Features (4), Examples (3), Footer (F9.14)"
```

---

### Task 2: Verify + PLAN.md + CHANGELOG + push

**Files:**
- Modify: `PLAN.md`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Final typecheck**

```bash
cd platform && npm run typecheck 2>&1 | tail -20
```

Expected: zero errors.

- [ ] **Step 2: Browser smoke test**

Otwórz `http://localhost:3000` (bez `/dev/`).

Sprawdź:
1. Sekcja Hero — widoczne "PortfolioHub" (logo), tagline, dwa przyciski: "Zobacz przykład →" i "Panel admina"
2. Sekcja "Co oferuje platforma?" — 4 karty w gridzie 2×2
3. Sekcja "Aktywne portfolio" — 3 karty z avatarami (złoty okrąg), nazwami, rolami i linkami
4. Footer — copyright + link "Panel admina"
5. Brak ThemeToggle, DownloadCvButton, CookieConsentBanner (slug = null) ✅
6. Tło: ciepły off-white (`#f5f3f2`) — `:root` light theme ✅

Jeśli serwer nie działa, odnotuj to i pomiń — nie uruchamiaj serwera.

- [ ] **Step 3: Mark F9.14 done in PLAN.md**

Znajdź:
```
- [ ] **F9.14** Landing page platformy (korp-cbm.com) z prezentacją możliwości
```
Zastąp:
```
- [x] **F9.14** Landing page platformy (korp-cbm.com) z prezentacją możliwości (2026-06-14, Agent: Claude)
```

- [ ] **Step 4: CHANGELOG — pod `## [Unreleased]` → `### Added`:**

```markdown
- `LandingPage` — landing page platformy na `korp-cbm.com`; sekcje: Hero (tagline + CTA), Funkcje (4 karty), Aktywne portfolio (3 przykłady), Footer; CSS Custom Properties `:root` light theme (F9.14)
```

- [ ] **Step 5: Commit i push**

```bash
git add /home/rspro/Dokumenty/1.CODE/2.Portfolio/PLAN.md /home/rspro/Dokumenty/1.CODE/2.Portfolio/CHANGELOG.md
git commit -m "docs(sync): mark F9.14 done — platform landing page"
git push origin dev
```

---

## Self-Review

### 1. Spec coverage
- F9.14: Landing page platformy (korp-cbm.com) ✅
- Prezentacja możliwości platformy ✅ (Features: 4 karty z opisami)
- Brak hardcoded domeny — `NEXT_PUBLIC_PLATFORM_DOMAIN` ✅

### 2. Placeholder scan
Brak. Wszystkie sekcje mają pełny kod: `FEATURES` i `EXAMPLES` są tablicami z rzeczywistą treścią, nie TODO.

### 3. Type consistency
- `LandingPage` — eksportowana jako nazwany export `export function LandingPage()` ✅
- Import w page.tsx: `import { LandingPage } from "@/components/landing/LandingPage"` ✅
- `process.env.NEXT_PUBLIC_PLATFORM_DOMAIN` → `string | undefined` → fallback `?? "korp-cbm.com"` → `string` ✅
- `FEATURES` map: key={f.title} — `title` jest unikalne w tablicy ✅
- `EXAMPLES` map: key={e.slug} — `slug` jest unikalne w tablicy ✅
- `new Date().getFullYear()` — Server Component, renderowane na serwerze przy build/request ✅
